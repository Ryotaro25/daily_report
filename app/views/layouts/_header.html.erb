<header class="navbar navbar-fixed-top navbar-default">
    <div class=" header">
      <%= link_to "Daily Report", root_path, id: "logo" %></span>
    </div>
    <nav>
     <ul class="nav navbar-nav navbar-right" id="list" >
        <li id="home">
          <%= link_to("ホーム", root_url) %>
        </li>
        <% if logged_in? %>
          <li class="members">
            <%= link_to("メンバーズ", users_path) %>
          </li>
          <li class="dropdown"> 
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">グループ<b class="caret"></b></a>
            <ul class=" dropdown-menu">
              <li class="groups">
                <%= link_to("グループ", groups_path) %>
              </li>
              <li id="accout">
              　<%= link_to("新規作成", new_group_path ) %>
            　</li>
            </ul>
          </li>
          <li class="dropdown"> 
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">アカウント<b class="caret"></b></a>
            <ul class=" dropdown-menu">
              <li id="accout">
                <%= link_to("プロフィール", current_user) %>
              </li>
              <li id="accout">
              　<%= link_to("設定", edit_user_path(current_user)) %>
            　</li>
              <li id="accout">
         　　　　 <%= link_to("ログアウト", logout_path, {method: :delete}) %>
        　　  </li>
            </ul>
          </li>
        <% else %>
          <li id="accout">
           <%= link_to("新規作成はこちら", signup_path, {class: "nav-link"})%>
          </li>
        <% end %>
      </ul>
    </nav>
</header>